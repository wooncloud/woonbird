import { like } from "../services/index.js";

const createLike = (req, res) => {
    let result = null;

    try {
      } catch (e) {
        console.error(e);
      }
    
      res.send(result);
};

const deleteLike = (req, res) => {
    let result = null;

    try {
      } catch (e) {
        console.error(e);
      }
    
      res.send(result);
};

export default {
    createLike, deleteLike
}